<% @items.each do |item| %>
Removed item:

  <%=raw item.attributes.to_yaml %>
broken dishes:
  <% item.dishes.each do |dish| %>
  #<%=raw dish.id %># <%=raw dish.name %>
  <% end %>
======================================================================

<% end %>
