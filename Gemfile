source 'https://rubygems.org'

gem 'rails'
# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem "mechanize"
gem "nokogiri"
gem "exception_notification", :require => 'exception_notifier'
gem "delayed_job_active_record"
gem "hirefire", :groups => [:development, :production]
gem "heroku"
gem "foreigner"
gem 'acts_as_tree', :git => "https://github.com/marceloribeiro/rails-acts_as_tree.git"
gem 'devise'

# databases
gem "pg", :groups => [:developpment, :production, :cucumber, :ci]

# testing tools
gem "rspec-rails", :groups => [:developpment, :test, :cucumber, :ci]
gem "rspec-spies", :groups => [:developpment, :test, :cucumber, :ci]
gem "factory_girl_rails", :groups => [:developpment, :test, :cucumber, :ci]
gem "ZenTest", :groups => [:developpment, :test, :cucumber, :ci]

gem "sqlite3-ruby", :require => "sqlite3", :group => :test

gem "cucumber-rails", :groups => [:cucumber, :ci]
gem "database_cleaner", :groups => [:cucumber, :ci]
gem "webrat", :groups => [:cucumber, :ci]


# Gems used only for assets and not required
# in production environments by default.
group :assets do
  # gem 'sass-rails',   '~> 3.2.3'
  # gem 'coffee-rails', '~> 3.2.1'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer'

  # gem 'uglifier', '>= 1.0.3'
end

# gem 'jquery-rails'

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'ruby-debug19', :require => 'ruby-debug'
